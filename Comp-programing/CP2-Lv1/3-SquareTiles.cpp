#include <bits/stdc++.h>

using namespace std;

int main(){
    int T; cin >> T;
    char a[64][64];
    for(int tc = 1; tc <= T; tc++){
        int row, col;
        cin >> row >> col;

        for(int i = 0; i < rol; i++){
            for(int j = 0; j < col; j++){
                cin >> a[i][j];
            }
        }
    }
}